@if($settings['enabled'] ?? true)<div class="theme-search-page-form theme-container" data-theme-block="search_form"><form action="{{ $canonicalSearchUrl }}" method="get" role="search"><label for="search-results-query">{{ $settings['label'] ?? 'جستجوی محصولات' }}</label><div><input id="search-results-query" name="q" type="search" value="{{ $searchQuery }}" placeholder="{{ $settings['placeholder'] ?? 'نام محصول را وارد کنید' }}"><button class="theme-button theme-button--primary" type="submit">جستجو</button>@if($searchQuery !== '')<a class="theme-button theme-button--outline" href="{{ $canonicalSearchUrl }}">پاک کردن</a>@endif</div></form></div>@endif

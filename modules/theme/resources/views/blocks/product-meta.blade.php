@php($tags = $product->tags ?? collect())
@if($product->category || $product->brand || $tags->isNotEmpty())<section class="theme-product-meta" data-theme-block="product_meta"><h2>{{ $settings['title'] ?? 'اطلاعات محصول' }}</h2><dl>@if($product->category)<div><dt>دسته‌بندی</dt><dd><a href="{{ route('product-categories.show', $product->category->slug ?: $product->category) }}">{{ $product->category->name }}</a></dd></div>@endif @if($product->brand)<div><dt>برند</dt><dd>{{ $product->brand->name }}</dd></div>@endif @if($tags->isNotEmpty())<div><dt>برچسب‌ها</dt><dd>{{ $tags->pluck('name')->join('، ') }}</dd></div>@endif</dl></section>@endif

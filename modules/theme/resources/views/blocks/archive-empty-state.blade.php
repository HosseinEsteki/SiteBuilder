@if($products->isEmpty())<section class="theme-empty theme-container" data-theme-block="archive_empty_state"><h2>{{ $settings['title'] ?? 'محصولی یافت نشد' }}</h2>@if($settings['description'] ?? null)<p>{{ $settings['description'] }}</p>@endif @if($settings['show_reset'] ?? true)<a href="{{ url()->current() }}">پاک کردن فیلترها</a>@endif @if($currentCategory)<a href="{{ route('theme.shop') }}">بازگشت به فروشگاه</a>@endif</section>@endif

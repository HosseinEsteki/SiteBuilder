@if($settings['show_header'] ?? true)<header class="theme-archive-header theme-search-header" data-theme-block="search_header"><x-theme::container><h1>{{ $settings['title'] ?? 'نتایج جستجو' }}</h1>@if($searchQuery !== '')<p>نتایج جستجو برای «{{ $searchQuery }}»</p>@elseif($settings['description'] ?? null)<p>{{ $settings['description'] }}</p>@endif @if($settings['show_result_count'] ?? true)<small>{{ number_format($resultCount) }} محصول</small>@endif</x-theme::container></header>@endif

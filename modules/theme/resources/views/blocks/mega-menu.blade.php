@if($settings['show_categories'] ?? true)<nav class="theme-mega" aria-label="منوی محصولات"><x-theme::container><div class="theme-mega__desktop">@foreach($categories as $category)<div><a class="theme-mega__parent" href="{{ route('product-categories.show', $category) }}">{{ $category->name }}</a>@foreach($category->themeChildren ?? [] as $child)<a href="{{ route('product-categories.show', $child) }}">{{ $child->name }}</a>@endforeach</div>@endforeach</div><div class="theme-mega__mobile">@foreach($categories as $category)<details><summary>{{ $category->name }}</summary><a href="{{ route('product-categories.show', $category) }}">مشاهده همه</a>@foreach($category->themeChildren ?? [] as $child)<a href="{{ route('product-categories.show', $child) }}">{{ $child->name }}</a>@endforeach</details>@endforeach</div></x-theme::container></nav>@endif

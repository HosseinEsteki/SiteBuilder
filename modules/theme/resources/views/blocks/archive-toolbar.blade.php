<div class="theme-archive-toolbar theme-container" data-theme-block="archive_toolbar">@if($settings['show_result_count'] ?? true)<span>{{ number_format($resultCount) }} نتیجه</span>@endif @if($settings['show_sorting'] ?? true)<form method="GET"><label for="archive-sort">مرتب‌سازی</label>@foreach(request()->except('sort', 'page') as $key => $value)@if(is_scalar($value))<input type="hidden" name="{{ $key }}" value="{{ $value }}">@endif @endforeach<select id="archive-sort" name="sort" onchange="this.form.submit()">@foreach($sortingOptions as $value => $label)<option value="{{ $value }}" @selected(($activeFilters['sort'] ?? 'newest') === $value)>{{ $label }}</option>@endforeach</select><noscript><button>مرتب‌سازی</button></noscript></form>@endif</div>

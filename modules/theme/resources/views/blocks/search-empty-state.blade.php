@if($products->isEmpty())<section class="theme-empty theme-container" data-theme-block="search_empty_state">@if($searchQuery === '')<h2>{{ $settings['empty_title'] ?? 'چه محصولی می‌خواهید؟' }}</h2><p>{{ $settings['empty_description'] ?? 'نام محصول را وارد کنید.' }}</p>@else<h2>{{ $settings['not_found_title'] ?? 'محصولی یافت نشد' }}</h2><p>{{ $settings['not_found_description'] ?? 'عبارت دیگری را امتحان کنید.' }}</p><p>برای «{{ $searchQuery }}» نتیجه‌ای پیدا نشد.</p><a href="{{ $canonicalSearchUrl }}">پاک کردن جستجو</a>@endif @if($settings['show_shop_action'] ?? true)<a href="{{ route('theme.shop') }}">بازگشت به فروشگاه</a>@endif</section>@endif
